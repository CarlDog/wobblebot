/*!
 * HTMX placeholder for WobbleBot dashboard (Stage 7.1.B).
 *
 * THIS IS A PLACEHOLDER. The operator must replace this file with a
 * real htmx.min.js before deploying the web UI to a setting where the
 * dashboard's polled cards (cost ledger, open orders) need to refresh
 * without a full page reload.
 *
 * Fetch the real HTMX:
 *
 *   curl -L --output src/wobblebot/web/static/htmx.min.js \
 *     https://unpkg.com/htmx.org@2.0.4/dist/htmx.min.js
 *
 * Pin to a major version (2.x); HTMX is stable but minor versions
 * occasionally adjust default attribute behavior. Verify SHA-256
 * against https://htmx.org/ when fetching.
 *
 * Without HTMX, the dashboard chrome (nav, login form, mutation
 * confirm pages) all work — only the auto-polled cards stay static
 * until a full-page reload.
 *
 * Stage 7.1.D's smoke walkthrough notes the placeholder status; a
 * follow-on test in Stage 7.5 verifies the operator has replaced the
 * file with one that contains the HTMX module signature.
 */
(function () {
  if (typeof console !== "undefined" && console.warn) {
    console.warn(
      "WobbleBot: htmx.min.js is a placeholder — replace with a real HTMX " +
        "build for polled-card auto-refresh. See the file header for the " +
        "fetch command."
    );
  }
})();
